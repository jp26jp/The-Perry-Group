---
pages:
- _posts/2018-02-20-how-to-win-a-multiple-offer-situation-in-utah.md
hide_body: false
fields:
- type: text
  name: layout
  label: Layout
  hidden: true
  default: post
- type: text
  name: title
  label: Title
  config:
    required: true
- type: tag_list
  name: tags
  label: Tags
  description: Give the post 2 or 3 tags. Press tab to save tag. Make sure all tags
    are in lowercase.
- type: select
  name: agent
  label: Agent
  config:
    required: true
    source:
      type: custom
    options:
    - Michael Perry
    - Mason Conley
    - Tim Leary
- type: select
  name: category
  label: Category
  description: If you need more categories, let me know.
  config:
    required: true
    source:
      type: custom
    options:
    - buyer
    - seller
- type: file
  name: image
  label: image
  config:
    required: true
